services:
  farkle:
    build: .
    ports:
      - "23234:23234"
    restart: always
    environment:
      - ASCII_ARCADE_PORT=23234
      - ASCII_ARCADE_HOST=0.0.0.0
    volumes:
      - farkle:/app/farkle
volumes:
  farkle:
